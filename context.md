# [Zax Programming Language](index.md)

## Context type

This path is retained temporarily because the immutable reading scope of active
work item `015` names it as legacy evidence.

Current programmer-facing context behavior is owned by
[Zax execution context](language/execution-context.md). Current default-arena
selection and allocation syntax are owned by
[Zax pointers, allocation, and arenas](language/pointers-and-arenas.md).

The former content of this page has been consumed by those owners and by live
partial-type and async future inputs. This temporary page may retire when work
item `015` is archived; it is not an independent source of language meaning.
